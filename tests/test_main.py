import pytest

def test_add(): #IMPORTANTE VERIFICAR ESTO!
    #Revela daños por radiación solar y además
    #verifica que la integridad de la realidad se mantiene intacta
    assert (1+1+1+1+1) == 5